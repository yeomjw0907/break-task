## Design System
Always read `DESIGN.md` before making visual or UI decisions.
All fonts, colors, spacing, motion, and component hierarchy for TaskBrick are defined there.
Do not introduce new visual patterns without checking them against `DESIGN.md`.
When reviewing UI, flag anything that breaks the design system or increases visual noise.
